# Firejail profile for bzip2
# Persistent local customizations
include /etc/firejail/bzip2.local
# Persistent global definitions
include /etc/firejail/globals.local

# Redirect
include /etc/firejail/gzip.profile
